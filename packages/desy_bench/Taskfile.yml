version: '3'

tasks:
  analyze:
    desc: Analyze the reusable Desy Bench package.
    cmds:
      - flutter analyze

  test:
    desc: Run Desy Bench package tests.
    cmds:
      - flutter test

  format:
    desc: Format Desy Bench source and tests.
    cmds:
      - dart format lib test
