name: desy_overlay
description: Debug-time widget selection and annotation for Flutter apps.
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.8.0
  flutter: ">=3.29.0"

dependencies:
  desy_design_system:
    path: ../desy_design_system
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
