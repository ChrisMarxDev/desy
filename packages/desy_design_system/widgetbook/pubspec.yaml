name: desy_widgetbook
description: Local Widgetbook experiment for comparing Desy and Widgetbook.
publish_to: none
version: 0.1.0+1

environment:
  sdk: ^3.8.0
  flutter: '>=3.44.0'

resolution: workspace

dependencies:
  desy_design_system:
    path: ..
  flutter:
    sdk: flutter
  widgetbook: ^3.25.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
