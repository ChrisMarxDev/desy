version: '3'

tasks:
  analyze:
    desc: Analyze the registry-derived Brand Guide extension.
    cmds:
      - flutter analyze

  test:
    desc: Run Brand Guide extension tests.
    cmds:
      - flutter test

  format:
    desc: Format Brand Guide extension source and tests.
    cmds:
      - dart format lib test
